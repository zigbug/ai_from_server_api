{
  "info": {
    "_postman_id": "ai-server-api-collection-hf",
    "name": "AI Server API — HF",
    "description": "Коллекция для работы целиком через Hugging Face без OpenRouter. GET /models подтягивает модели напрямую с Hugging Face Hub по категориям text/image/edit с флагом free (бесплатность). См. docs/openapi.yaml.",
    "schema": "https://schema.getpostman.com/json/collection/v2.1.0/collection.json"
  },
  "item": [
    {
      "name": "Health",
      "description": "Состояние сервера",
      "item": [
        {
          "name": "GET / (info)",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/",
              "host": ["{{baseUrl}}"],
              "path": [""]
            }
          }
        },
        {
          "name": "GET /health",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/health",
              "host": ["{{baseUrl}}"],
              "path": ["health"]
            }
          }
        },
        {
          "name": "GET /version",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/version",
              "host": ["{{baseUrl}}"],
              "path": ["version"]
            }
          }
        }
      ]
    },
    {
      "name": "Models",
      "description": "Список доступных моделей с Hugging Face Hub (кэш 1 час). Каждая модель содержит free — бесплатна ли она. ?refresh=1 сбрасывает кэш.",
      "item": [
        {
          "name": "GET /models (список моделей)",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/models",
              "host": ["{{baseUrl}}"],
              "path": ["models"]
            }
          }
        },
        {
          "name": "GET /models?refresh=1 (обновить кэш)",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/models?refresh=1",
              "host": ["{{baseUrl}}"],
              "path": ["models"],
              "query": [
                {
                  "key": "refresh",
                  "value": "1"
                }
              ]
            }
          }
        }
      ]
    },
    {
      "name": "Sessions",
      "description": "Сессии диалогов: создание, листинг, история, сообщения.\n\nИнструкция:\n1) Создайте сессию (POST /sessions) и скопируйте session_id.\n2) Вставьте его в переменную окружения sessionId.\n3) Остальные запросы сессии заработают.",
      "item": [
        {
          "name": "POST /sessions (создать)",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "try {",
                  "  const text = pm.response.text();",
                  "  if (!text) throw new Error('empty');",
                  "  const jsonData = JSON.parse(text);",
                  "  const newId = jsonData && (jsonData.id || jsonData.session_id);",
                  "  if (newId) {",
                  "    pm.environment.set('sessionId', newId);",
                  "    console.log('sessionId set to:', newId);",
                  "  }",
                  "} catch (e) {",
                  "  console.log('Response is not JSON, skipping sessionId extraction:', e.message);",
                  "}"
                ]
              }
            }
          ],
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"name\": \"Игра 'Мастер подземелий'\",\n  \"model\": \"{{modelText}}\",\n  \"system_prompt\": \"Ты — мастер подземелий. Отвечай кратко и атмосферно.\"\n}"
            },
            "url": {
              "raw": "{{baseUrl}}/sessions",
              "host": ["{{baseUrl}}"],
              "path": ["sessions"]
            }
          }
        },
        {
          "name": "GET /sessions (список)",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/sessions",
              "host": ["{{baseUrl}}"],
              "path": ["sessions"]
            }
          }
        },
        {
          "name": "GET /sessions/{id} (по ID)",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/sessions/{{sessionId}}",
              "host": ["{{baseUrl}}"],
              "path": ["sessions", "{{sessionId}}"]
            }
          }
        },
        {
          "name": "PATCH /sessions/{id} (обновить)",
          "request": {
            "method": "PATCH",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"name\": \"Переименованная сессия\"\n}"
            },
            "url": {
              "raw": "{{baseUrl}}/sessions/{{sessionId}}",
              "host": ["{{baseUrl}}"],
              "path": ["sessions", "{{sessionId}}"]
            }
          }
        },
        {
          "name": "DELETE /sessions/{id} (удалить)",
          "request": {
            "method": "DELETE",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/sessions/{{sessionId}}",
              "host": ["{{baseUrl}}"],
              "path": ["sessions", "{{sessionId}}"]
            }
          }
        },
        {
          "name": "GET /sessions/{id}/history",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/sessions/{{sessionId}}/history",
              "host": ["{{baseUrl}}"],
              "path": ["sessions", "{{sessionId}}", "history"]
            }
          }
        },
        {
          "name": "POST /sessions/{id}/messages (отправить сообщение)",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"message\": \"Привет! Расскажи анекдот\"\n}"
            },
            "url": {
              "raw": "{{baseUrl}}/sessions/{{sessionId}}/messages",
              "host": ["{{baseUrl}}"],
              "path": ["sessions", "{{sessionId}}", "messages"]
            }
          }
        }
      ]
    },
    {
      "name": "Images",
      "description": "Генерация и редактирование изображений",
      "item": [
        {
          "name": "POST /images (сгенерировать)",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"prompt\": \"кот в скафандре\",\n  \"model\": \"{{modelImage}}\"\n}"
            },
            "url": {
              "raw": "{{baseUrl}}/images",
              "host": ["{{baseUrl}}"],
              "path": ["images"]
            }
          }
        },
        {
          "name": "POST /images/edit (отредактировать)",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"prompt\": \"сделай это фото в стиле киберпанк\",\n  \"image\": \"<base64 исходной картинки>\",\n  \"model\": \"{{modelEdit}}\",\n  \"negative_prompt\": \"размытость, шум\",\n  \"guidance_scale\": 7.5,\n  \"num_inference_steps\": 30,\n  \"width\": 512,\n  \"height\": 512\n}"
            },
            "url": {
              "raw": "{{baseUrl}}/images/edit",
              "host": ["{{baseUrl}}"],
              "path": ["images", "edit"]
            }
          }
        }
      ]
    }
  ],
  "variable": []
}