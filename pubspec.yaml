name: ai_from_server_api
description: A server app using the shelf package and Docker. Backend for AI chat/image generation serving Flutter apps.
version: 1.1.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.11.5

dependencies:
  shelf: ^1.4.2
  shelf_router: ^1.1.2
  http: ^1.2.2
  sqlite3: ^2.4.6
  uuid: ^4.5.1
  logging: ^1.2.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
